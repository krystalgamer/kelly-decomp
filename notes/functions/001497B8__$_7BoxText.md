# _$_7BoxText

- Address: `0x001497B8`
- Size: `0xDC` (220 bytes)
- Object: `game/files_frontend`
- Debug source: `C:/KS/SRC/ks/FEPanel.cpp`
- Reference source: `KS/SRC/ks/FEPanel.cpp`
- Result: **deferred**

## Attempts

| # | Status | Byte score | Instruction score | Candidate |
| ---: | --- | ---: | ---: | --- |
| 1 | different | 13.6364 | 5.4545 | `candidate.cpp` |
| 2 | different | 13.6364 | 5.4545 | `candidate.cpp` |
| 3 | different | 13.6364 | 5.4545 | `candidate.cpp` |
| 4 | different | 32.9545 | 22.7273 | `candidate.cpp` |
| 5 | different | 42.7273 | 32.7273 | `candidate.cpp` |

### Attempt 1 notes

Attempt 1 used the exact released `BoxText::~BoxText` body with the shared FEPanel declarations. The shared header's `#pragma interface` prevented destructor inlining: GCC called `_$_10StringList` and tail-called `_$_10TextString`, producing 152 bytes instead of 220.

### Attempt 2 notes

Attempt 2 removed `#pragma interface` and recreated the exact member layout, but GCC still emitted standalone `_$_10StringList` and `_$_10TextString` helpers. The scored destructor remained byte-for-byte equivalent to attempt 1 at 152 bytes.

### Attempt 3 notes

Attempt 3 added the original key-virtual context and an explicit inline `StringList` destructor. GCC still chose the same two out-of-line destructor calls, so the result and score were unchanged.

### Attempt 4 notes

Attempt 4 explicitly lowered the released destructor in C++ to expose the target vtable stores, vector-cookie walk, string destruction, and deleting-destructor flag. It matched the 96-byte frame and opening register saves, but duplicated common-path setup and tail-called `__builtin_delete`, yielding 264 bytes.

### Attempt 5 notes

Attempt 5 retained the explicit C++ lowering but kept the vector pointer in ABI register `$5`, preserved the base-vtable high half across the array loop with instruction-free compiler constraints, and blocked the deleting call's tail-call optimization. This reduced the candidate to 212 bytes and produced the best result: 42.7273% bytes, 32.7273% instructions, status `different`.

## Outcome

Deferred after five source-level attempts. The BoxText teardown layout and destructor chain were reconstructed, but the best candidate remained 212 bytes with substantially different inlining and register scheduling.
