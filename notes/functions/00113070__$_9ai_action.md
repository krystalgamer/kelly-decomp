# _$_9ai_action

- Address: `0x00113070`
- Size: `0x30` (48 bytes)
- Object: `game/files_ai`
- Debug source: `C:/KS/SRC/ai_actions.h`
- Reference source: `KS/SRC/ai_actions.h`
- Result: **matched**

## Attempts

| # | Status | Byte score | Instruction score | Candidate |
| ---: | --- | ---: | ---: | --- |
| 1 | different | 14.5833 | 8.3333 | `candidate.cpp` |
| 2 | matched | 100.0 | 100.0 | `candidate.cpp` |

### Attempt 2 notes

The released generated destructor restores the vtable at offset 0xc and conditionally calls `__builtin_delete`. The carried vtable alias reproduces the negative low half, and the trailing empty barrier prevents a sibling delete call.

`KELLY_DECOMP_COMPILER_BARRIER()` is a matching-only annotation that emits no target instruction. It prevents EE GCC from applying the sibling/tail-call or scheduling transformation described above.

## Outcome

The released `ai_action` destructor matched exactly on attempt two.
