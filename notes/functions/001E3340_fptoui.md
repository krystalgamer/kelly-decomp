# fptoui

- Address: `0x001E3340`
- Size: `0x28` (40 bytes)
- Object: `game/files_hwosps2`
- Debug source: `C:/KS/SRC/ps2main.cpp`
- Reference source: `KS/SRC/ps2main.cpp`
- Result: **matched**

## Attempts

| # | Status | Byte score | Instruction score | Candidate |
| ---: | --- | ---: | ---: | --- |
| 1 | different | 65.0 | 50.0 | `candidate.cpp` |
| 2 | matched | 100.0 | 100.0 | `candidate.cpp` |

### Attempt 2 notes

The released function uses the original PS2 `FTOI(float)` inline asm after EE GCC converts the double argument through `dptofp`; preserving that helper keeps the converted word in `$f0` and reproduces the `v1` return copy.

## Outcome

The released `fptoui` helper matched exactly.
