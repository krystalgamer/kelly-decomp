{
  "address": "0x00336048",
  "attempts": [
    {
      "attempt": 1,
      "candidate_file": "candidate.cpp",
      "candidate_sha1": "edc2cd2a0e993fc6ca92b30cc9f60a8b9f5bee9b",
      "notes": "# FIRST PASS wave 151 lane A2\n\nExact released source tested once for `0x00336048 open__10chunk_fileRC7stringxi`. Attempts before: 0. Result `compile_failed`, score 0.0000%. No retry, follow-up, diff chase, build, finalize, integration, git operation, or tracked edit.",
      "score": 0.0,
      "source": "typedef unsigned long big_int;\nclass stringx;\nclass string_buf { friend class stringx; friend stringx operator+(const stringx&,const stringx&); big_int *data; int ref_count,char_length,block_length,max_blocks; };\nclass stringx { friend stringx operator+(const stringx&,const stringx&); char *chars; string_buf *my_buf; public: stringx(); stringx(const stringx&); ~stringx(); void append(const stringx&); void make_room(int); const char *c_str() const { return chars; } int length() const { return my_buf->char_length; } };\ninline stringx operator+(const stringx&a,const stringx&b){stringx r;r.make_room(a.my_buf->char_length+b.my_buf->char_length);r.append(a);r.append(b);return r;}\nclass filespec { public: stringx path,name,ext; explicit filespec(const stringx&); };\nclass os_file { stringx name; int flags; bool opened,eof,from_cd; char implementation[32]; public: enum { FILE_READ=1,FILE_WRITE=2 }; void open(const stringx&,int); bool is_open() const { return opened; } int read(void*,int,bool=false); void close(); int write(const void*,int); };\nclass text_file { char data[144]; public: void open(const stringx&,int); };\nclass multistash { char padding[708]; public: bool stash_file_is_open; char rest[83]; };\nclass stash { void *index; bool opened,eof; unsigned fp,max_fp; char fullname[32]; public: static int curstash; static multistash substash[5]; static bool is_stash_open(){return substash[curstash].stash_file_is_open;} static int get_current_stash(){return curstash;} static void set_current_stash(int s){curstash=s;} static bool file_exists(const char*); bool open(const char*); unsigned read(void*,int); void close(); };\nclass os_developer_options { public: enum { FLAG_CHATTY_LOAD,FLAG_STASH_ONLY }; static os_developer_options *inst(); bool is_flagged(int) const; };\nvoid debug_print(const char*,...); void error(const stringx&);\nclass chunk_file { public: enum { MODE_MASK=0xff,FILE_TEXT=0x100,FILE_BINARY=0x200 }; enum chunk_file_t { CFT_NONE,CFT_TEXT,CFT_BINARY }; bool use_stash; private: chunk_file_t type; os_file binary; text_file text; stash the_stash; int my_stash; public: void open(const stringx&,int); };\n__asm__(\".equ __7stringx,0x0034D3E0\"); __asm__(\".equ _$_7stringx,0x0034D6E0\"); __asm__(\".equ make_room__7stringxi,0x0034E220\"); __asm__(\".equ append__7stringxRC7stringx,0x0034E528\"); __asm__(\".equ __8filespecRC7stringx,0x00336B80\"); __asm__(\".equ curstash__5stash,0x0045D9C0\"); __asm__(\".equ substash__5stash,0x0045B7B8\");\n#define FBIN_TAG_VALUE 0x4642494E\n#define PSTRING_MAX_LENGTH 40\nvoid chunk_file::open( const stringx& name, int flags )\n{\n  int theOldStash = the_stash.get_current_stash();\n  stringx temp_filename;\n  int tag;\n  int mode = flags & MODE_MASK;\n  if ( mode==os_file::FILE_READ && !(flags&FILE_TEXT) )\n  {\n    if ( stash::is_stash_open() )\n    {\n      filespec spec(name);\n      temp_filename = spec.name + spec.ext;\n      if ( temp_filename.length() > PSTRING_MAX_LENGTH )\n      {\n      }\n      if ( temp_filename.length() <= PSTRING_MAX_LENGTH && the_stash.file_exists(temp_filename.c_str()) )\n      {\n        use_stash = true;\n        my_stash = the_stash.get_current_stash();\n        the_stash.open(temp_filename.c_str());\n        the_stash.read(&tag, sizeof(tag));\n      }\n    }\n    type = CFT_BINARY;\n    if (use_stash == false)\n    {\n      if (os_developer_options::inst()->is_flagged(os_developer_options::FLAG_CHATTY_LOAD))\n        debug_print(\"Going to disk for file %s\\n\", name.c_str());\n      if (os_developer_options::inst()->is_flagged(os_developer_options::FLAG_STASH_ONLY))\n      {\n      }\n      binary.open( name, mode );\n      if (binary.is_open() ==false)\n      {\n        return;\n      }\n      binary.read( &tag, sizeof(tag) );\n    }\n    if ( tag != FBIN_TAG_VALUE )\n    {\n      if ( flags & FILE_BINARY )\n        error( name + \": binary chunk_file tag not found\" );\n      if (use_stash == true)\n      {\n        the_stash.close();\n        use_stash = false;\n      }\n      else\n        binary.close();\n      type = CFT_TEXT;\n      text.open( name, mode );\n    }\n  }\n  else if ( flags & FILE_BINARY )\n  {\n    binary.open( name, mode );\n    if ( mode == os_file::FILE_WRITE )\n    {\n      tag = FBIN_TAG_VALUE;\n      binary.write( &tag, sizeof(tag) );\n    }\n  }\n  else\n  {\n    type = CFT_TEXT;\n    text.open( name, mode );\n  }\n  the_stash.set_current_stash(theOldStash);\n}\n",
      "status": "compile_failed"
    }
  ],
  "raw_name": "open__10chunk_fileRC7stringxi"
}
