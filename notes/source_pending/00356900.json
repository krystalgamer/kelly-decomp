{
  "address": "0x00356900",
  "attempts": [
    {
      "attempt": 1,
      "candidate_file": "candidate.cpp",
      "candidate_sha1": "2cca8220098e314bd5c1df50348e17a53f7ea8a9",
      "notes": "# FIRST PASS wave 105 lane B4\n\nExact released body tested once for `0x00356900 filter__10zip_filterP7os_fileiPvi`. Result `compile_failed`, score 0.0000%. No retry, variant, diff chase, build, finalize, integration, git mutation, or tracked edit.",
      "score": 0.0,
      "source": "typedef unsigned int u_int;\ntypedef void *voidpf;\ntypedef void (*free_func)(voidpf, voidpf);\ntypedef voidpf (*alloc_func)(voidpf, u_int, u_int);\n\nstruct z_stream\n{\n  unsigned char *next_in;\n  u_int avail_in;\n  unsigned long total_in;\n  unsigned char *next_out;\n  u_int avail_out;\n  unsigned long total_out;\n  char *msg;\n  void *state;\n  alloc_func zalloc;\n  free_func zfree;\n  voidpf opaque;\n  int data_type;\n  unsigned long adler;\n  unsigned long reserved;\n};\n\nclass os_file\n{\npublic:\n  int read(void *, int, bool);\n};\n\nextern \"C\" int sprintf(char *, const char *, ...);\nextern \"C\" int inflate(z_stream *, int);\nextern \"C\" int inflateEnd(z_stream *);\nextern \"C\" int inflateInit_(z_stream *, const char *, int);\nextern \"C\" int sceCdSync(int);\nvoid *zip_filter_alloc(void *, unsigned int, unsigned int);\nvoid zip_filter_free(void *, void *);\n\n#define MAX_INPUT_WINDOW_SIZE (128 * 1024 + 1)\n#define Z_SYNC_FLUSH 2\n#define Z_OK 0\n#define Z_STREAM_END 1\n#define ZLIB_VERSION \"1.1.3\"\n#define inflateInit(strm) inflateInit_((strm), ZLIB_VERSION, sizeof(z_stream))\n#define assert(x) ((void)0)\n#define verify(x) ((void)0)\n#define NEXT_READ in_remaining -= ( this_read = ( ( in_remaining > (int) readSize ) ? readSize : in_remaining ) )\n\nclass zip_filter\n{\npublic:\n  static int filter(os_file *io, int in_remaining, void *data, int out_size);\nprivate:\n  static unsigned char input_window[];\n  static z_stream zip_stream;\n  static unsigned int readSize;\n};\n\nint zip_filter::filter(os_file *io, int in_remaining, void *data, int out_size)\n{\n  unsigned char *input_window_1 = input_window;\n  unsigned char *input_window_2 = input_window + ( MAX_INPUT_WINDOW_SIZE );\n  int ret_val;\n  int this_read = 0;\n  int actually_read;\n  char error_txt[20];\n  zip_stream.zfree = zip_filter_free;\n  zip_stream.zalloc = zip_filter_alloc;\n  \n  zip_stream.next_out = (unsigned char*) data;\n  zip_stream.avail_out = out_size;\n  zip_stream.next_in = NULL;\n\n  inflateInit( &zip_stream );\n  \n  NEXT_READ;\n  actually_read = io->read( input_window_1, this_read, false );\n  sprintf(error_txt, \"%d\", actually_read);\n  verify(actually_read == this_read);\n  while(1)\n  {\n    assert(zip_stream.avail_in == 0);\n    zip_stream.next_in = input_window_1;\n    zip_stream.avail_in = this_read;\n    NEXT_READ;\n    if( this_read )\n    {\n      actually_read = io->read( input_window_2, this_read, true );\n      sprintf(error_txt, \"%d\", actually_read);\n\t    verify(actually_read == this_read);\n    }\n    else\n    {\n#ifdef TARGET_PS2\n      sceCdSync(0);\n#endif // TARGET_PS2\n    }\n    ret_val = inflate( &zip_stream, Z_SYNC_FLUSH );\n    sprintf(error_txt, \"%d\", ret_val);\n    assert(ret_val == Z_OK || ret_val == Z_STREAM_END);\n\n    if( this_read )\n    {\n    assert(zip_stream.avail_in == 0);\n      zip_stream.next_in = input_window_2;\n      zip_stream.avail_in = this_read;\n      NEXT_READ;\n      if( this_read )\n      {\n        actually_read = io->read( input_window_1, this_read, true );\n        sprintf(error_txt, \"%d\", actually_read);\n\t\t    verify(actually_read == this_read);\n      } \n      else\n      {\n#ifdef TARGET_PS2\n        sceCdSync(0);\n#endif // TARGET_PS2\n      }\n      ret_val = inflate( &zip_stream, Z_SYNC_FLUSH );\n      sprintf(error_txt, \"%d\", ret_val);\n      assert(ret_val == Z_OK || ret_val == Z_STREAM_END);\n\n      if( !this_read )\n        break;\n    }\n    else\n      break;\n  }\n\n  inflateEnd( &zip_stream );\n\n  return zip_stream.total_out;\n}\n",
      "status": "compile_failed"
    }
  ],
  "raw_name": "filter__10zip_filterP7os_fileiPvi"
}
