{
  "address": "0x00336BD0",
  "attempts": [
    {
      "attempt": 1,
      "candidate_file": "candidate.cpp",
      "candidate_sha1": "b2f375725e0e9975448303a9f2c3c5bfda1f33c8",
      "notes": "Attempt 1 used the exact released filespec::_extract body with only the released global.h/filespec.h declarations. The harness compile failed because those headers are not on its include path; per first-pass instructions, no retry or variant was attempted.",
      "score": 0.0,
      "source": "#include \"global.h\"\n#include \"filespec.h\"\n\nvoid filespec::_extract(const stringx & src)\n  {\n  int cp1=src.rfind('\\\\');\n  if (cp1!=stringx::npos)\n    {\n    cp1++;\n    path = src.substr(0,cp1);\n    }\n  else\n    {\n    path = empty_string;\n    cp1=0;\n    }\n\n  int cp2 = src.rfind('.');\n  if (cp2!=stringx::npos)\n    {\n    name = src.substr(cp1,cp2-cp1);\n    ext = src.substr(cp2,src.length()-cp2);\n    }\n  else\n    {\n    name = src.substr(cp1,src.length()-cp1+1);\n    ext = empty_string;\n    }\n  }\n",
      "status": "compile_failed"
    }
  ],
  "raw_name": "_extract__8filespecRC7stringx"
}
